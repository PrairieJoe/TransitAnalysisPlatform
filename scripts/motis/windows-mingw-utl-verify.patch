diff --git a/deps/utl/include/utl/verify.h b/deps/utl/include/utl/verify.h
--- a/deps/utl/include/utl/verify.h
+++ b/deps/utl/include/utl/verify.h
@@ -32,7 +32,7 @@ Ex fail(std::string_view msg, FmtArgs... args) {
 
   auto const now = clock::to_time_t(clock::now());
   struct tm tmp;
-#if _MSC_VER >= 1400
+#if defined(_MSC_VER) || defined(_WIN32)
   gmtime_s(&tmp, &now);
 #else
   gmtime_r(&now, &tmp);
